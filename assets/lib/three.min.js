// Three.js库占位符
// 实际应用中应该从 https://threejs.org/ 下载完整的three.min.js

// 简单的Three.js模拟
window.THREE = {
    Scene: function() {
        this.background = null;
        this.fog = null;
        this.add = function() {};
        this.remove = function() {};
    },

    PerspectiveCamera: function() {
        this.position = { x: 0, y: 0, z: 0 };
        this.lookAt = function() {};
    },

    WebGLRenderer: function() {
        this.setSize = function() {};
        this.setPixelRatio = function() {};
        this.shadowMap = { enabled: false };
        this.shadowMapType = 0;
        this.domElement = document.createElement('div');
    },

    BoxGeometry: function() {},

    PlaneGeometry: function() {},

    MeshLambertMaterial: function() {},

    Mesh: function() {},

    GridHelper: function() {},

    AmbientLight: function() {},

    DirectionalLight: function() {},

    Fog: function() {},

    Color: function() {
        this.isColor = true;
    },

    Vector2: function() {
        this.x = 0;
        this.y = 0;
    },

    Raycaster: function() {},

    GLTFLoader: function() {
        this.load = function(url, onLoad, onProgress, onError) {
            // 模拟加载
            setTimeout(() => {
                if (onError) {
                    onError(new Error('Three.js库为占位符，请下载完整版本'));
                }
            }, 1000);
        };
    },

    OrbitControls: function() {
        this.enableDamping = false;
        this.dampingFactor = 0;
        this.minDistance = 0;
        this.maxDistance = 0;
        this.maxPolarAngle = 0;
    },

    PCFSoftShadowMap: 1
};

console.warn('Three.js库为占位符，请从 https://threejs.org/ 下载完整版本');
